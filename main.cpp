#include <iostream>
int main(int argc, char *argv[]) {
    // input an initial position and a series of moving commands
    // initial position in the format of (x,y,theta)
    //command in the format of (r,d), d = moving length and r = rotation



    return 0;
}
